# Fixture Notes — 02-limit-history

Fixture: `linear-5`

Shape: five commits with controlled dates.

Required properties:

- commit dates span both sides of `2024-01-03`
- newest two commits are known
- date filters have deterministic expected result sets

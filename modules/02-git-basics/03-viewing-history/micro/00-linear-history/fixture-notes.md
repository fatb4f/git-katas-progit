# Fixture Notes — 00-linear-history

Fixture: `linear-5`

Shape: five commits on one branch.

Required properties:

- newest and oldest commits have stable expected hashes or fixture aliases
- newest commit has a known author and subject
- history is reachable from `HEAD` with no merge commits

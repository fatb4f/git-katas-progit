# Fixture Notes — 09-message-author-date-filter

Fixture: `metadata-varied`

Shape: commits with varied authors, dates, and messages.

Required properties:

- at least one commit author matches `Ada`
- at least one commit message matches `release`
- commit dates make `2024-01-02` through `2024-01-04` meaningful

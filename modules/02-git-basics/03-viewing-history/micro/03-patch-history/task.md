# Task — 03-patch-history

Use the prepared repository fixture. Do not mutate history unless the task explicitly asks for it.

Run the primary command family:

```bash
git log -p
git show
```

Write:

- commit hash that added the target line to `.answers/commit_that_added_line`
- commit hash that removed the target line to `.answers/commit_that_removed_line`

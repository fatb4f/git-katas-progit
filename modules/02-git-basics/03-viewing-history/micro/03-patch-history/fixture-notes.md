# Fixture Notes — 03-patch-history

Fixture: `linear-5`

Shape: commits with small text changes.

Required properties:

- one commit adds a target line
- one later commit removes that target line
- patches are small enough to inspect without noise

# Fixture Notes — 07-graph-history

Fixture: `branch-merge`

Shape: two branches and one merge commit.

Required properties:

- `HEAD` is attached to a named branch
- at least two branch tips are visible with `--all`
- one merge commit has two parents

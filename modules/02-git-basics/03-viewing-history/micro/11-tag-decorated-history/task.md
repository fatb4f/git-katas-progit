# Task — 11-tag-decorated-history

Use the prepared repository fixture. Do not mutate history unless the task explicitly asks for it.

Run the primary command family:

```bash
git log --decorate
git log v1.0..HEAD
```

Write:

- commit hash decorated with `v1.0` to `.answers/tagged_commit`
- commits after `v1.0` to `.answers/commits_after_tag`

# Fixture Notes — 11-tag-decorated-history

Fixture: `tagged-release`

Shape: several commits with two tags.

Required properties:

- tag `v1.0` points to a known commit
- commits exist after `v1.0`
- decorations are visible with `git log --decorate`

# Fixture Notes — 10-pickaxe-history

Fixture: `symbol-lifecycle`

Shape: symbol introduced, changed, and removed.

Required properties:

- `featureFlag` is introduced in one commit
- `featureFlag` is removed in a later commit
- intermediate commits do not obscure the pickaxe result

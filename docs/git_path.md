|   Layer | Learning target                   | Material type                      | Primary materials                                             | Commands / concepts                                                                      | Kata / project output                                                              | Evidence gate                                               |
| ------: | --------------------------------- | ---------------------------------- | ------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------- |
|   **0** | Git mental model                  | Book                               | **Pro Git**, especially object model and internals            | blob, tree, commit, tag, ref, HEAD, index, worktree                                      | Draw object graph for a repo after 3 commits and 2 branches                        | Can explain `worktree → index → tree → commit → ref`        |
|   **1** | Classic porcelain reflexes        | Book + drills                      | **Pro Git** + **eficode git-katas**                           | `add`, `commit`, `status`, `diff`, `log`, `branch`, `merge`, `rebase`, `reset`, `reflog` | Complete classic staging/branching/reset/reflog katas                              | Can recover from bad reset using reflog                     |
|   **2** | Safer modern porcelain            | Manpages + custom katas            | `git-scm` command docs                                        | `switch`, `restore`, `worktree`, `stash`, `clean`                                        | Replace old `checkout` habits with explicit `switch` / `restore` workflows         | Can state which command mutates branch vs file state        |
|   **3** | Patch-series workflow             | Manpages + custom katas            | `range-diff`, rebase docs, release notes                      | `rebase -i`, `rebase --update-refs`, `range-diff`, `cherry-pick`, `format-patch`         | Create v1/v2 patch series and review with `range-diff`                             | Can compare two versions of a stack without plain `diff`    |
| **3.5** | Modern history surgery            | Release notes + manpages           | Git 2.54 notes, `git history`, `git replay`                   | `git history reword`, `git history split`, `git replay`, bare-repo rewrites              | Split a commit and preserve descendant branch refs                                 | Can explain when to use `history` vs `rebase -i`            |
|  **4A** | Worktree isolation                | Manpages + repo exercises          | `git worktree` docs                                           | linked worktrees, per-worktree `HEAD`, per-worktree index, task branches                 | One branch per task in separate worktrees                                          | Can run two task branches without stash abuse               |
|  **4B** | Sparse working sets               | Manpages + engineering blogs       | `git sparse-checkout`, GitHub sparse-checkout article         | cone mode, sparse index, skip-worktree, path cones                                       | Clone a monorepo-like repo into role-specific sparse cones                         | Can prove only selected paths are populated                 |
|  **4C** | Partial clone / promisor model    | Technical docs + engineering blogs | partial clone docs, sparse + partial examples                 | `--filter=blob:none`, promisor remote, lazy blob fetch                                   | Read only selected paths from remote history                                       | Can show when blobs are fetched on demand                   |
|  **4D** | Repository maintenance            | Manpages + release notes           | `git maintenance`, Git release notes                          | commit-graph, MIDX, incremental repack, geometric repack, loose objects                  | Benchmark repo before/after maintenance                                            | Can identify which maintenance task changed pack layout     |
|  **4E** | Large-object / pack introspection | Technical docs                     | Git technical docs: commit-graph, multi-pack-index, packfiles | `verify-pack`, `count-objects`, `multi-pack-index`, `commit-graph`                       | Inspect object database shape before/after repack                                  | Can locate largest reachable objects                        |
|  **5A** | Plumbing as tool substrate        | Pro Git Ch. 10 + manpages          | Pro Git internals                                             | `cat-file`, `hash-object`, `update-index`, `write-tree`, `commit-tree`, `update-ref`     | Create a commit manually without porcelain                                         | Commit appears in `git log` and has expected tree           |
|  **5B** | Machine-readable repo evidence    | Manpages + custom tooling          | `git repo`, `git last-modified`, plumbing docs                | `repo info -z`, `repo structure -z`, `last-modified -z`, `for-each-ref --format`         | Emit JSON evidence for refs, object format, repo shape, file last-modified commits | Output is parseable, stable, and testable                   |
|  **5C** | Hook / policy control             | Release notes + hook docs          | `githooks`, config-based hooks notes                          | traditional hooks, `core.hooksPath`, config hooks, pre-mutation gates                    | Centralize a policy hook across repos                                              | Hook behavior is reproducible from config                   |
|  **5D** | Adapter / automation layer        | Build-your-own tool                | Go/Python + Git CLI or library                                | subprocess contract, NUL parsing, batch `cat-file`, ref transactions                     | Build `gitfacts`: repo facts → JSON/CUE                                            | Tool never parses human tables                              |
|   **6** | Workflow control plane            | Custom curriculum                  | Your CUE/RALPH/agentflow model                                | task graph, worktree isolation, mutation gates, evidence manifests                       | Agent executes task branch and emits manifest                                      | Manifest proves inputs, mutation, validation, and promotion |
